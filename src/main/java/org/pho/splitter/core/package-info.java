package org.pho.splitter.core;

/**
 * This project was built to easy the way that big strings with no separation marks
 * can be separated into an entity respecting their field types.
 * <p>
 * The package <code>builders</code> contains the builders for the project.
 * <p>
 * The package <code>exception</code> contains the exceptions that are thrown
 * by the classes when some error occur.
 * <p>
 * The package <code>function</code> contains the functions that are used in the project
 * classes.
 * <p>
 * The package <code>splits</code> contains all the splits for Java primitive types and
 * other classes.
 *
 * @author Paulo Henrique Ortolan
 */
